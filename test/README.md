# 벽 자동 인식 회귀 가드

`wall-snapshots.json` = 픽스처 도면별 벽 인식 결과의 **좌표 단위 스냅샷**
(각 항목: "fromX,fromZ,toX,toZ,두께[,창N]" 정렬 목록).

## 프로토콜
- trace.js를 변경하면 픽스처 전체를 재실행해 이 스냅샷과 **좌표 단위로** 대조한다.
  개수 비교만으로는 재구성이 은폐된다(실사례: 밴드 룰 변경이 CAD 벽을 재구성했지만
  개수가 우연히 같아 가드를 통과했음).
- 차이가 나면: (1) 의도된 개선인지 판별하고 근거와 함께 스냅샷 갱신, (2) 아니면 롤백.
- 실전 지표는 별도: 합성 하드셋(ai-auto/도면샘플/실전풍_*) recall/precision + LH 배치 커버리지.

픽스처 원본: ai-auto/도면샘플/ (버전 관리 밖). widthCm은 스냅샷에 기록됨.

## v2(trace2.js) GT 랩 벤치 프로토콜 (2026-07-12 구축)

정답 있는 합성 하드셋에서 v1/v2를 같은 심판으로 채점한다.

1. GT 랩: `ai-auto/도면샘플/실전풍_생성.py` → `실전풍_아파트_850px.jpg` + `실전풍_아파트_GT.json`
2. 평가기: `ai-auto/도면샘플/aihub_평가/eval_segments.py <GT.json> <검출.json> --assumed-width <가정폭>`
   (세그먼트 vs 세그먼트, 1px=1cm 래스터, tol ±6cm. 보정 ±20% 실험은 --assumed-width로 재스케일)
3. 실행: `npm run dev` 후 headless Chrome에서
   `(await import('/src/lib/trace2.js')).detectWalls2('/bench.jpg', {x:0, z:0, widthCm})`
   (이미지는 public/에 임시 복사, 끝나면 삭제. debug=true 3번째 인자로 라벨맵 PNG·방/포켓/리지 통계)
4. 채점 3종: 정보정 1300 / -20% 1040 / +20% 1560.

2026-07-12 기록 (P/R):
- v1: 0.792/1.000 · 0.855/1.000 · 0.915/1.000
- v2(봉인 세대): 0.949/0.948 · 0.885/0.842 · 0.955/0.944
- v2(워터셰드 세대): 0.949/0.955 · 0.903/0.941 · 0.954/0.949 — f1 0.952/0.922/0.952
- v2(안장 병합 세대): 0.949/0.955 · 0.930/0.941 · 0.873/0.949 — f1 0.952/0.936/0.910
  (1560 하락 = 주방 카운터+상단벽 사이 띠 경유 경계가 t60 벽으로 — 물리적 barrier라 반쯤 정당,
   GT 기준으론 감점. 원인 규명됨: 카운터가 구조 마스크에 들고 위 틈 띠가 주방 라벨로 흘러
   상단벽(외부 소유)과 카운터 사이에 리지가 섬)
- v2(최급상승+톤게이트 세대): f1 0.952/0.904/0.952 — 가로/세로 비대칭 해소(원인=버킷 홍수의
  왼쪽-우선 채택이 벽 평행 등고선 행을 스캔 방향으로 찢던 것 → 최급상승 채택으로 교체).
  가구 그늘 분지의 가짜 벽(소파 t58·수납 t50)은 톤 게이트(회색 블롭 제거)+리지 판정을
  structural 마스크로 통일해 해소. 1040 잔여 2건=카운터 t75·기둥 두께오염 t45(국소·규명됨).
- v2(중심선 스냅 세대): f1 0.952/0.890/0.952 — 리지 픽셀을 구조 띠 중심선으로 스냅해
  소유권 경계의 대각 계단 파편화 해소(기장 가로벽이 전장으로 조립, 가로 10/세로 6).
  구조 필터의 선형 심볼 판정을 표본 중앙값 두께로 교체(합성물 방어).
- v2(형상 게이트 세대): f1 0.964/0.948/0.964, **P=1.000 전 보정** — 1040 아티팩트 3건 전부
  규명·종결: ①t45(침실2벽)=교차부 조각이 mergeSegs max 두께로 본벽 오염→주 세그먼트 두께
  유지+두께 상한 초과 세그 폐기 ②t45(글자 슬래브)=닫힘이 점선·글자를 벽 네트워크에 이어붙여
  성분 필터가 원리적으로 무력(성분=이미지 전체!)→세그먼트 국소 종횡비(길이<두께×1.5=블롭)
  게이트 ③카운터 t75=같은 게이트에 소멸. 선형 심볼 성분 필터도 열별 최대 연속 런 중앙값으로
  강화(이중 라인 방어).
- v2(띠 연속성 세대): f1 0.966/0.935/0.967, 기장 16·의정부 37 — 종횡비 게이트에 "같은 띠가
  세그먼트 너머로 이어지면(한쪽 70%+) 벽" 예외를 추가해 라벨쌍별로 쪼개진 두꺼운 실전 벽
  토막 회수. 카운터 t75만 1040에서 복귀(물리적 barrier·국소 감점, 수용).
- v2(암부 바닥 재분류 세대): GT 동일(0.966/0.935/0.967)·기장 16·의정부 37 — ①75cm 두꺼운
  암부 열림을 structural 차감에서 **raw 차감으로 승격**(내부가 바닥이 되어 분지·포켓 형성
  가능 — 파우더룸 분지 복원 ✅, GT 침대 완전 투명화) ②중간회색 전용 열림(게이트 lo+0.15Δ,
  50cm) 추가 — 이번 샘플엔 무영향이지만 밝은 타일 시트 대비.
  코너 잔여의 최종 정체(톤 프로브 실측): 균질 타일이 아니라 **잉크 톤(≈47)의 붙박이장
  블록 + 회백 혼합** — 톤·기하 어느 게이트로도 벽과 분리 불가 → v2 한계로 수용(물리적으론
  barrier이기도 함).
- v2(창 패스 세대, 7/13): GT 유지(0.966/0.935/0.967, 1300·1560 P=1.000)·기장 18(가로 12)·
  의정부 37 — ①label 0 사각지대(새시 이중선 사이 띠)를 phase 4 후 이웃 다수결 흡수(쌍 생성)
  ②얇은/바닥 경계에 창 시그니처: 수직 프로파일(±32cm)의 가는 선(≤8cm) 개수 — **30퍼센타일
  ≥2**(중앙값이면 확장 점선 쌍이 창으로 오승격 — 새시=연속선 vs 점선=듀티 60%가 결정적 차이)
  → 창 벽 승격(t=선들 스팬). 주방 상단 외벽 전장·침실3 하단 새시 등 회수.
- v2(외부흡수+창스키마, 7/13): GT 유지 — 방-외부 병합 허용(여백 치수띠 분지 흡수),
  창 세그먼트는 openings[{type:'window'}]로 분리 출력(기장 창2·의정부 창1, GT 창0=정상).
v2 남은 과제: ①침실1-거실 벽 하부·좌측 외벽 부분 미검출 ②CubiCasa5K 대규모 승격 평가
③픽스처 9종(사무실·CAD·손그림 등) 전면전 — v2 미측정 영역. 기장 18 vs v1 79(잉크 가구
코너=수용, 나머지 갭은 ①과 파편 조립 한계).
